# --- Training (train.py) ---
datasets
evaluate
seqeval
accelerate

# --- Model + tokenizer (shared by training and backend) ---
transformers
torch

# --- Backend API (backend.py) ---
fastapi
uvicorn
pydantic